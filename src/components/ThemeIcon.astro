---
import ThemeToggleButton from "./theme/ThemeToggleButton.astro";

interface Props {
  class?: string;
}
---

<ThemeToggleButton />
<style>
  .theme-toggle input[type="checkbox"] {
    display: none;
  }
</style>
<!-- <script is:inline>
  document
    .getElementById("setTheme")
    .addEventListener("click", () => theme.setTheme("auto"));
  const themeElement = document.getElementById("getTheme");

  if (themeElement) {
    themeElement.innerHTML = theme.getTheme();
  }

  document.addEventListener("theme-changed", () => {
    const resultsElem = document.getElementById("getTheme");
    resultsElem && (resultsElem.innerHTML = theme.getTheme());
  });
</script> -->
